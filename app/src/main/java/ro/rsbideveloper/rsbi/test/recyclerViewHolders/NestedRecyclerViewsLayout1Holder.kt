package ro.rsbideveloper.rsbi.test.recyclerViewHolders

import android.view.View
import androidx.recyclerview.widget.RecyclerView

// <TODO> is this effectively just a binding class ? if so, this is just stupid; but I think that it's slightly more than that,
    // because it allows maybe setting specific event listeners for each item (but isn't their "information access" more restricted
    // than the Adapter ? if so, this is useless indeed)
class NestedRecyclerViewsLayout1Holder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    //
}